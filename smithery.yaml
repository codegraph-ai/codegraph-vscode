startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      workspace:
        type: string
        description: Path to workspace directory. Optional — defaults to current working directory.
  command: npx
  args:
    - -y
    - "@memoryx/codegraph-mcp"
